<%= form_for @comment do |f| %>
 <%= f.hidden_field :user_id %>
 <%= f.hidden_field :post_id %>
  <div class="field">
    <%= f.label :コメントを書く %>
    <%= f.text_area :body, id: :comment_body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
